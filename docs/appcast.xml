<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
  <channel>
    <title>Omelette</title>
    <link>https://adxd-og.github.io/usage-checker/appcast.xml</link>
    <description>Omelette release feed</description>
    <language>en</language>
    <item>
      <title>Omelette v2.2.1</title>
      <pubDate>Thu, 03 Sep 2026 11:43:47 +0300</pubDate>
      <sparkle:version>33</sparkle:version>
      <sparkle:shortVersionString>2.2.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Fixed</h3>
<ul>
<li>The <code>omelette-hook</code> helper retries its wait when a signal interrupts it during the decision window (a stop/continue of the terminal's process group, say). It could otherwise give up and hand the prompt back to the terminal while Omelette still showed <b>Allow</b> / <b>Deny</b>.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v2.2.1">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v2.2.1/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="j1TASsAMmP7oCJy/05G+/LSNCUKDdaKbmSph0a3RckRLeqNKuqvQuQGMs3T/CcWskCOC6SEH6tzrSaERF2q9CA=="
        length="16050193"/>
    </item>
    <item>
      <title>Omelette v2.2.0</title>
      <pubDate>Wed, 02 Sep 2026 23:02:07 +0300</pubDate>
      <sparkle:version>32</sparkle:version>
      <sparkle:shortVersionString>2.2.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Approve or deny Claude Code from Omelette.</b> When a session asks permission and the terminal running it isn't the app you're looking at, the notification carries <b>Allow</b> and <b>Deny</b>, and so does that session's row in the popover. When the terminal *is* in front, nothing changes: Claude Code asks where it always has.</li>
<li>Held requests are answered at most once, expire after two minutes, live only in memory, and are released to the terminal untouched on any timeout, quit, disconnect or malformed reply — Omelette never allows anything by itself.</li>
<li>Settings → Agents has a switch for it, with counts of the requests pending, answered, expired and released to the terminal.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The <code>PermissionRequest</code> hook now waits up to 150 seconds instead of 5, so there is time to answer. Settings → Agents will say **Installed — older than this build<b> once; one </b>Update** click rewrites the entry.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v2.2.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v2.2.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="cxUYV3O6bhp+leebpxvtpUMeRAIL/S4IiO8Za/N1paMm982obGtE2B9pbcCjtS4zqQ+/JeKBgrutqz/NLNQAAw=="
        length="16022518"/>
    </item>
    <item>
      <title>Omelette v2.1.0</title>
      <pubDate>Wed, 02 Sep 2026 15:43:49 +0300</pubDate>
      <sparkle:version>31</sparkle:version>
      <sparkle:shortVersionString>2.1.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>An Agents tab in the dashboard.</b> Live sessions and the run history the app has been recording since 2.0.0: a summary strip (sessions, agent time, approval requests, busiest project) over the range you pick, the live list, and finished sessions grouped by day with their project, start time, duration and turns. Filter by source — All · Claude · Codex.</li>
<li>The agent run history is trimmed to the last 90 days once per launch, so <code>agent-sessions.jsonl</code> can't grow without a bound.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The dashboard, Settings, the welcome tour, the floating window and the desktop widgets are drawn with the design system the popover got in 2.0.0 — the same ring gauges, bars, section headers, status chips and battery colours. The widgets now share the app's ring and bar code instead of keeping their own.</li>
<li>The floating window shows the leading window as a ring with the next two under it, and how many agent sessions are running — amber when one needs you.</li>
<li>The dashboard remembers its last tab, and on the Agents tab ⌘1 · ⌘2 · ⌘3 switch the All · Claude · Codex source filter.</li>
<li>Cost accounting keeps a cache on disk (<code>cost-cache-claude-v1.json</code> in Application Support): relaunching no longer re-reads every Claude Code transcript before the $ figures appear, and the minute poll opens only files that changed.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v2.1.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v2.1.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="OX0v3qOM2repicGh4tdKff6eMp6xVTg007eMmZmAINs/ApedwOHcF2mpldAnow/KUBrrwD+qdqBksO5pPBFzBA=="
        length="15974588"/>
    </item>
    <item>
      <title>Omelette v2.0.0</title>
      <pubDate>Wed, 02 Sep 2026 11:15:58 +0300</pubDate>
      <sparkle:version>30</sparkle:version>
      <sparkle:shortVersionString>2.0.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Agent Overview.</b> The popover lists your live Claude Code and Codex sessions — grouped *Needs you / Working / Done / Idle* on the All tab, flat on a provider tab — with the project, what the agent is doing, and how long it has been in that state. Clicking a row brings its terminal tab back to the front (Terminal.app and iTerm2 select the exact tab; other hosts are activated; sessions without a host open their folder in Finder).</li>
<li>A menu-bar <b>agents pill</b>: grey with the session count when quiet, blue while agents work, amber "N needs you" when one waits for approval. Toggle in Settings → Agents.</li>
<li><b>Notifications</b> when a session needs your approval (with an <b>Open</b> action, replaces rather than stacks, withdrawn once you answer; ignores quiet hours by default) and, opt-in, when a turn finishes.</li>
<li><b>Settings → Agents</b>: one-click install/remove of the Claude Code hooks in <code>~/.claude/settings.json</code> and the Codex <code>notify</code> line in <code>~/.codex/config.toml</code>, with the exact JSON/TOML shown first, foreign entries preserved, a one-time <code>.omelette-backup</code>, a conflict state when another tool owns Codex's <code>notify</code>, and diagnostics (socket, helper version, events received/dropped, last event). The welcome tour gained one optional card for the same switch.</li>
<li>Without hooks, sessions are still listed from the CLIs' own logs (marked "≈", never "needs you"); hook data wins once installed. If Claude Code is on the machine and the hooks are not, the popover offers them once (Enable / Not now) and a single notification does the same at launch; nothing is written without a click.</li>
<li>A tiny <code>omelette-hook</code> helper ships inside the app and talks to it over a local Unix socket (0600, 64 KB cap). It sends only session id, tool name, a truncated tool summary, folder and host process info — never prompts or file contents — and always exits 0 within 0.8 s, so agents never wait on it.</li>
<li>Finished sessions are summarised to <code>agent-sessions.jsonl</code> (project, start, end, turns, approvals) for the upcoming dashboard history.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>New popover: an <b>All</b> tab with a tile per provider (ring for the leading window, bar for the weekly, plan) and a 7-day cost tile; provider tabs with a large ring for the most-constrained window, the other session window as a row, weekly windows as rings, extra usage and cost rows. <code>All</code> is the default tab. With more than four providers the segments go icon-only.</li>
<li>Design system (<code>UI/DesignSystem/</code>): tokens, ring gauge with pace marker, segmented control, tiles, hero, section headers, chips.</li>
<li>Usage colours are now battery-style everywhere — popover, dashboard, menu-bar pills and the desktop widgets: green while comfortable, amber from 70 %, red from 90 % (was accent blue below 70 %).</li>
</ul>
<h3>Internal</h3>
<ul>
<li><code>WindowRanking</code> and <code>BurnVerdict</code> extracted from the popover and unit-tested.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v2.0.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v2.0.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="IjxRIdTnVPN+r+gtobU3NvWTsKlKrQcZAl+VgQzMQn4EtbTlH6qI1T2A5F5Hnph+s8WI4YQ6Xf6LH0Bb30PpDw=="
        length="16360045"/>
    </item>
    <item>
      <title>Omelette v1.13.1</title>
      <pubDate>Mon, 31 Aug 2026 23:14:02 +0300</pubDate>
      <sparkle:version>29</sparkle:version>
      <sparkle:shortVersionString>1.13.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Fixed</h3>
<ul>
<li>Claude went signed-out roughly once per token lifetime and only came back after pressing "Request keychain access now". A keychain ACL trusts <em>binaries</em>, so the rename to Omelette dropped the app off <code>Claude Code-credentials</code>' trust list: with the prompt suppressed (1.12.0's silent keychain) its background read could only ever fail, leaving the app's own cache as the sole source and nothing able to refresh it. The item's ACL does trust <code>/usr/bin/security</code> — the tool Claude Code writes it with — so the read is now delegated there when the direct one fails, guarded by an ACL preflight so an untrusted <code>security</code> is never invoked and can't raise a panel.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.13.1">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.13.1/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="TEYsOR3Oet4LvFWWw4SY5JuA7DJhK4sSwv32iaUNpvSdqg7MwiVlG8ucaztIbWh+nfauOSqnpE5f2748zHNgBQ=="
        length="15345522"/>
    </item>
    <item>
      <title>Omelette v1.13.0</title>
      <pubDate>Fri, 28 Aug 2026 15:56:50 +0300</pubDate>
      <sparkle:version>28</sparkle:version>
      <sparkle:shortVersionString>1.13.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Quota over time for providers without a cost log.</b> Antigravity and Gemini keep no local token log and bill by subscription, so a dollar figure can't exist for them — but consumption can. Their dashboard tabs now chart the quota history the app has been recording since 1.12.0: - <b>History</b> — one line per window on a fixed 0–100% axis (rescaling would hide the headroom), binned by time keeping each bin's *peak*, plus a table of daily peaks. - <b>Activity</b> — a day grid coloured by the day's peak utilization; a day the app wasn't running is shown as unobserved, not as 0%. - <b>Insights</b> — days at capacity (≥95%: providers round and stop updating at the cap), average daily peak, busiest day, today's peak, and average daily *consumption* — the sum of positive increments, so a window driven to 60% twice counts as 120% spent, taken over the largest window only so a session window and the weekly it rolls into aren't billed twice. History-only windows (ones the provider has stopped reporting) keep their line, end early, and never colour the grid. 24 new tests (200 total).</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The "no cost log" note for such providers now reads "Quota over time is charted instead" and sits as a footnote beside the chart.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.13.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.13.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="Mjyrcr64wMpy2zCfDBFYOHskvHOfzC7lXrLshRcY9Epb3RboKV62SM3SmAUgrJr3rhXNgRTx0QewSny8c0T/CQ=="
        length="15324164"/>
    </item>
    <item>
      <title>Omelette v1.12.1</title>
      <pubDate>Fri, 28 Aug 2026 12:08:44 +0300</pubDate>
      <sparkle:version>27</sparkle:version>
      <sparkle:shortVersionString>1.12.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<p>Patch from a five-part external code review of 1.12.0 (keychain, aggregators,</p>
<p>notifier, dashboard, test quality), every finding verified against the code</p>
<p>before being fixed and several refuted. Tests: 96 → 176.</p>
<h3>Fixed</h3>
<ul>
<li><b>Keychain writes are gated like reads.</b> <code>save()</code>/<code>clear()</code> on the credentials cache checked nothing; with a locked login keychain and a <code>~/.claude/.credentials.json</code> present, a background poll could still raise the password panel through the cache *write*. A user who clicks Deny on the interactive dialog now gets the permission message, not "status -128".</li>
<li><b>A 401 can't replace the cache with an older token.</b> The renewal read after a 401 accepted whatever Claude Code's sources held — a stale credentials file could overwrite the newer cached token.</li>
<li><b>Burn rate survives a session reset.</b> The prediction used the first and last points of the 30-minute lookback; after a reset (92% → 4% → 38%) the delta was negative and "burning fast" stayed silent for up to half an hour — exactly when it matters. The rate is now measured on the window that is open now. Staleness is also reachable again (10 minutes), and the minimum legal 30-second spacing between points counts.</li>
<li><b>Threshold alerts don't re-fire on a rounding wobble</b> (80 → 79 → 80). The fired level clears only after usage drops a few points below the threshold.</li>
<li><b>Claude Code's last log line is never lost.</b> The Claude aggregator marked a partially written last line as consumed; it now advances only past the last newline (as the Grok aggregator already did). A line whose timestamp can't be parsed is dropped instead of being billed as "now".</li>
<li><b>Grok costs: <code>costUsdTicks: 0</code> means unpriced, not free</b>, negative token counts are clamped, and a turn total larger than its per-model split is no longer silently discarded.</li>
<li>**Dashboard selection can't paint one provider's costs under another's title.** Switching providers mid-scan discarded nothing; a refresh started for the previous selection could publish after the switch. Refreshes now carry the service and generation they were started for.</li>
<li><b>A Grok-only user is no longer stuck on Claude in the dashboard</b>, and a provider disabled in Settings is no longer selectable there. The popover's burn-rate verdict follows the popover's own tab, not the dashboard's.</li>
<li><b>Reset all settings also resets</b> the dashboard and popover selections, the peek shortcut, remembered alert levels, and Launch at Login.</li>
<li><b>The last visible provider can't be hidden from the menu bar.</b></li>
<li><b>Unfunded dollar pools no longer appear as windows.</b> The usage payload carries codenamed pools (<code>nimbus_quill</code> and friends) with dollar fields; one of them reported <code>utilization: 0</code> and showed up as a 0% window. A pool renders only when it has a limit. The daily-summary day key is a local calendar day (the GMT rendering was fragile across timezone changes).</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Test suite hardened per the review: a tautological test removed, two coincidental fixtures replaced with ones where the alternatives disagree, <code>NotNil</code> assertions replaced with exact values, boundary and guard cases added for pacing, burn rate, payload decoding, history, pricing parse, and the notifier; new <code>UsageSnapshot</code> tests for the headline rule. Every group mutation-checked.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.12.1">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.12.1/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="ufaxqjULtROyCvylHrm/c42uqiF5/nAs599+f/Jg9jxz3EaXU+crCuA4ou8sB3us+5TSRcro2nHNPUSoOJGOAQ=="
        length="15210995"/>
    </item>
    <item>
      <title>Omelette v1.12.0</title>
      <pubDate>Fri, 28 Aug 2026 11:13:34 +0300</pubDate>
      <sparkle:version>26</sparkle:version>
      <sparkle:shortVersionString>1.12.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<p>The keychain release. The permission dialog that came back every few hours</p>
<p>was never the ACL reset the code assumed — Claude Code *updates* its item and</p>
<p>has since April. Two other things were prompting, and this release measures</p>
<p>rather than guesses: what actually silences a background keychain read on</p>
<p>macOS 26, and what can't be silenced at all (a locked login keychain — so we</p>
<p>stop asking). Also the first unit-test suite: 96 tests, each group verified to</p>
<p>go red when its logic is broken.</p>
<h3>Fixed</h3>
<ul>
<li><b>Background polling never shows a keychain dialog.</b> Every background read runs with keychain UI disabled at the process level and is skipped outright while the login keychain is locked. <code>LAContext.interactionNotAllowed</code> and <code>kSecUseAuthenticationUIFail</code> — the previous approach, and CodexBar's — were measured to still raise the legacy Allow/Deny panel; they stay as a belt-and-braces layer, but the guarantee comes from <code>SecKeychainSetUserInteractionAllowed</code>. The only reads allowed to prompt are the onboarding <b>Grant access</b> button and Settings → **Request keychain access now**.</li>
<li><b>Omelette no longer refreshes the Anthropic token itself.</b> Claude Code rotates its refresh token, so a refresh from a second app either stole the CLI's session or lost the race and killed ours — that race, not an ACL, was the "every ~8 hours" cadence. Credentials are now read-only: own cache → <code>~/.claude/.credentials.json</code> → silent probe of Claude Code's item. A 401 no longer clears the cache (on machines without the credentials file it was the only copy), it re-reads Claude Code's sources once and otherwise shows a signed-out state with a hint to run <code>claude</code>.</li>
<li><b>Gemini's threshold notifications fire.</b> Providers whose every window is model-scoped (Gemini's daily Pro/Flash quotas) had nothing left to alert on after the model-scoped filter; those windows are now watchable when they're all a provider has.</li>
<li><b>Gemini's pace indicator is right.</b> Window length is carried in the model (from CodexBarCore's <code>windowMinutes</code>) instead of being inferred as "weekly" for every model-scoped window; Gemini's are daily.</li>
<li><b>Threshold alerts don't replay after a restart.</b> Fired levels persist.</li>
<li><b>Pay-as-you-go accounts appear in the widget</b> — a spend limit renders as a window, and a windowless account shows its 7-day CLI spend instead of being filtered out entirely.</li>
<li><b>A 401 response body never reaches the UI.</b> Error text is a short status description; the body goes to the log only.</li>
<li><b>Reset all settings resets all settings</b> — providers, budget, menu bar options, session alerts and onboarding included, behind a confirmation. A single <code>SettingsStore.Defaults</code> is the source of truth for every default.</li>
<li>Settings → Account describes each provider in plain words ("Session 42% · Week 18%") instead of "N buckets". Onboarding and Settings no longer promise off-peak reminders that didn't exist. README no longer claims the keychain dialog appears exactly once.</li>
<li><code>signing.xcconfig.example</code> now produces a notarizable build (<code>ENABLE_HARDENED_RUNTIME</code> + <code>OTHER_CODE_SIGN_FLAGS</code>), and <code>project.yml</code> no longer sets a project-level <code>ENABLE_HARDENED_RUNTIME = NO</code> that outranked the xcconfig.</li>
</ul>
<h3>Added</h3>
<ul>
<li><b>Session-timing alerts.</b> "Burning fast" fires when, at the current pace, the session window hits 100% *before* it resets — a fast pace that resets in time is deliberately silent. "Resets in N min" fires in the last 15 minutes of a window you're already pressed against. Each nudges once per window; lead time is configurable in Settings → Notifications.</li>
<li><b>"Current session window" in Insights</b> — what ran while the open session window filled: cost, turns, models and projects from the CLI logs. Honest about its limits: when the log shows nothing, it says the usage came from elsewhere (the Claude apps, another machine) rather than implying nothing happened.</li>
<li><b>Grok costs in the dashboard.</b> A new incremental aggregator reads the Grok CLI's session logs (<code>~/.grok/sessions</code>), using the CLI's own <code>costUsdTicks</code> as the cost — no price table involved — with per-model and per-project breakdowns. Cold scan of 400 MB of logs: 0.66 s; subsequent polls: 0.04 s. The popover gains a "Last 7 days" row for Grok.</li>
<li><b>Dashboard and floating window for every provider.</b> History is recorded for each provider (old records read as Claude), with a service picker in the dashboard header. Where a provider has no local cost log — Antigravity keeps none, Codex's logs give only totals — the dashboard says exactly that instead of a generic "Claude only".</li>
<li><b>Menu bar options</b> — choose which providers occupy the menu bar, and whether the percentage shows always, never, or only with a single provider.</li>
<li><b>Global "peek at usage" shortcut</b> (Settings → General), via KeyboardShortcuts. Unset by default.</li>
<li><b>models.dev pricing now covers xAI and Google</b> models, alongside Anthropic and OpenAI. Models without a published price (image/video) are skipped; models known only to models.dev keep a readable display name.</li>
<li><b>Unit-test target</b> (<code>UsageTrackerTests</code>, XCTest, 96 tests): pacing and burn-rate rules, both log aggregators on fixture logs, the Claude usage payload decoder, notifier bucket selection, window lengths, history round-trips and per-service throttling, settings reset, error descriptions, pricing parse. The app's launch side effects are gated off under XCTest.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Onboarding asks for keychain access with an explicit <b>Grant access</b> button and shows whether it was granted — since background polling never prompts, the dialog has to be requested.</li>
<li>User-Agent for the usage endpoint tracks the current Claude Code release.</li>
<li>The unused <code>writeBack</code> into Claude Code's keychain item and the <code>OAuthRefreshClient</code> are gone.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.12.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.12.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="h+ZQecQQnO/Ao+9ueEaWKg9oNCM/xHzONCO4PLiZHv7tjVbmwxXHSRthSZR2wwVuE2gvWa6uI/Mw0pCwxTRIDA=="
        length="15183225"/>
    </item>
    <item>
      <title>Omelette v1.11.0</title>
      <pubDate>Tue, 04 Aug 2026 11:42:41 +0300</pubDate>
      <sparkle:version>25</sparkle:version>
      <sparkle:shortVersionString>1.11.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<p>Performance release: an Opus 5 audit of a 4.8-day-uptime process (4.75% constant</p>
<p>CPU, 1.34 GB peak memory) traced every hot spot; all of them are fixed here.</p>
<p>Verified against the previous code on real logs — every cost figure matches to</p>
<p>the cent, first-scan peak memory drops from 1134 MB to 302 MB.</p>
<h3>Fixed</h3>
<ul>
<li><b>Menu bar no longer redraws twice a second forever.</b> The critical-state pulse animation drove a continuous redraw loop even in the normal state, for every visible provider — the main source of the constant CPU burn. The animation timeline now exists only while a window is actually ≥95%.</li>
<li><b>History no longer rewrites the whole file every minute.</b> Usage history is now an append-only JSONL log (~300 bytes per poll instead of re-serializing megabytes — about 9.5 GB of disk writes per day at 90 days of history). Existing <code>history.json</code> migrates automatically; the migration is rollback-safe (an older build's file, being a complete rewrite, supersedes the log when it's newer).</li>
<li><b>First scan of Claude Code logs no longer spikes memory past 1 GB.</b> Session files are parsed and folded one at a time; turns older than a month collapse into per-day aggregates instead of being held forever; the dedup set keeps 8-byte hashes instead of id strings.</li>
<li>**The poll path stopped hauling the full history array onto the main actor every minute.** Burn rate reads only the last half hour from the store; the dashboard loads the full history only while its window is open (and now refreshes live while it is).</li>
<li><b>Polling pauses during sleep and screen lock</b> — no more waking CLI subprocesses to update a menu bar nobody can see — and refreshes immediately on wake/unlock.</li>
<li><b>Changing the refresh interval applies immediately</b> instead of after the old interval elapsed one last time.</li>
<li><b>Codex session parsing is incremental</b>: the active session file has only its new tail read on each poll (carrying the cumulative-counter baseline), and cache entries for files that aged out of the window are evicted.</li>
<li>Removed a redundant 10-second tooltip timer (the snapshot notification already covers it); narrowed the popover header's clock tick to the "Updated Xs ago" text; Insights recompute off the main thread only when their inputs change; model-pricing normalization and project-name resolution are memoized off the per-turn hot path.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.11.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.11.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="CcT6H9sFTrkJk28pCcBNwyZJKrdr5/9fuuy3yOeTXx7X+GPzQ+CULn9ph7kW9oGA/djdyRwjkl4ozG0f7PseBQ=="
        length="14773951"/>
    </item>
    <item>
      <title>Omelette v1.10.0</title>
      <pubDate>Mon, 13 Jul 2026 16:18:58 +0300</pubDate>
      <sparkle:version>24</sparkle:version>
      <sparkle:shortVersionString>1.10.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Provider tabs in the popover.</b> One provider on screen at a time, switched by a row of brand-logo tabs with battery-style status dots — no more stacking every card. The selection is remembered; with a single provider enabled the popover looks exactly as before, and the hero ring now speaks for the selected provider in multi-provider mode.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><b>Model-scoped windows inform, never drive.</b> "Fable only" / "Opus only" caps keep their card row but no longer move the menu-bar percent, the hero ring, tab dots, the widget ring, or threshold notifications — the all-models weekly is "the" limit.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.10.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.10.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="swNEv0tSGakGsziyh4y+Xu3AksdrEjeKiPHTM0j0cQZ4Cd4kUijnay/2g/0hqUQ+Eh7nPAqTIB6FvnvXX6eDCw=="
        length="14751781"/>
    </item>
    <item>
      <title>Omelette v1.9.0</title>
      <pubDate>Mon, 13 Jul 2026 15:03:41 +0300</pubDate>
      <sparkle:version>23</sparkle:version>
      <sparkle:shortVersionString>1.9.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Model-scoped Claude limits (Fable).</b> The usage endpoint's new <code>limits</code> array is now parsed, so model-scoped weekly windows — like Fable's cap — appear as their own rows, drive the menu-bar headline when they're the binding limit, and flow into the widget and history. Windows appear and disappear with the server payload; the next scoped model needs no app update.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.9.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.9.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="Kmqa4SCpzikmrC3paFK+y9TXPMEPhyXRT/yrf1pktxwoELD/vRFkRR8u/+eTNJSGmgF64wIw9lz3A6dEbXilBQ=="
        length="14709580"/>
    </item>
    <item>
      <title>Omelette v1.8.1</title>
      <pubDate>Sun, 12 Jul 2026 19:20:13 +0300</pubDate>
      <sparkle:version>22</sparkle:version>
      <sparkle:shortVersionString>1.8.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul>
<li><b>Provider cards show real brand logos.</b> The popover, Settings list, and desktop widget now render each provider's monochrome logo (Claude, Codex, Gemini, Antigravity, Grok) instead of generic system symbols. Logos are tinted like the old icons and fall back to a system symbol for entries without one (e.g. the Anthropic admin card).</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.8.1">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.8.1/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="+dFOiB+A4Fleft5TL8qOm8OkdU/ULrf0/PL1gFH5U+imWybgPXYZ/mWYt/nKFhPjCj4CEk4Fcy9xhxBXzWsIDg=="
        length="14701762"/>
    </item>
    <item>
      <title>Omelette v1.8.0</title>
      <pubDate>Sun, 12 Jul 2026 18:21:41 +0300</pubDate>
      <sparkle:version>21</sparkle:version>
      <sparkle:shortVersionString>1.8.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Grok (xAI) usage tracking.</b> A new provider card shows billing-period credit usage from the local Grok CLI (<code>grok agent stdio</code> RPC), falling back to grok.com web billing when the CLI is unavailable. Includes a Settings toggle (on by default when the Grok CLI is signed in), a sign-in recovery popover (<code>grok login</code> + dashboard link), and Grok as a selectable provider in the desktop widget.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.8.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.8.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="xCf3Zad+18cKqulGM7HyRX3vKeAMpeVhw1J2Oa4P7ReghQ5HkDaAYhIBOwNtN18jHqSPQ2gJeDuoUiJbXhL+Cg=="
        length="14633588"/>
    </item>
    <item>
      <title>Omelette v1.7.7</title>
      <pubDate>Sat, 11 Jul 2026 20:03:10 +0300</pubDate>
      <sparkle:version>20</sparkle:version>
      <sparkle:shortVersionString>1.7.7</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul>
<li><b>The popover header shows the app icon</b> (with several providers) instead of a generic gauge symbol.</li>
<li><b>The menu bar tooltip covers every provider</b> — it was hardcoded to Claude; now each provider with data gets its own block, plus the $ figure for pay-as-you-go.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.7">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.7/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="72bt428TehMev1DhGt/qQHwu2HNnzAZlCQMM7NKs4+kAZ73Fhhwt4L0qG8s1MskHgQVDbnjg+O1Mdxo3iTunDw=="
        length="14621677"/>
    </item>
    <item>
      <title>Omelette v1.7.6</title>
      <pubDate>Sat, 11 Jul 2026 19:50:24 +0300</pubDate>
      <sparkle:version>19</sparkle:version>
      <sparkle:shortVersionString>1.7.6</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul>
<li><b>The Dashboard now says "Claude data only".</b> Its charts are built from Claude usage history and Claude Code session logs; the sidebar badge makes that honest instead of implying every provider is included.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.6">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.6/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="Sxe6McCKLD9PGs/lmM4RfOZ0beRznGCAFJASSycgcx+pFEgaVgBJjt33v3e82rBoDJk2wdqWpO6i6tqCIrtJDA=="
        length="14633934"/>
    </item>
    <item>
      <title>Omelette v1.7.5</title>
      <pubDate>Sat, 11 Jul 2026 19:35:20 +0300</pubDate>
      <sparkle:version>18</sparkle:version>
      <sparkle:shortVersionString>1.7.5</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Fixed</h3>
<ul>
<li><b>Re-enabling a provider brings it back immediately.</b> A user-initiated refresh (provider toggles, Refresh buttons, keychain button) was silently dropped when a poll was already in flight or a 429 backoff was active — the provider then stayed hidden until the next timer tick, which with a 5-minute interval felt like forever. User requests now coalesce into a guaranteed follow-up poll and bypass the backoff; the timer still honors Retry-After.</li>
<li>The keychain-access button status no longer claims "refreshing…" forever — it confirms and clears itself.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.5">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.5/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="yjpCo9BhljcGv9y5QjlBvg1ysRCsMW+reJK7dq/LVOaF3UgtgwnD06Z1KwyvxgZPKgBZPBNwGymYs1ePZCe2BQ=="
        length="14605984"/>
    </item>
    <item>
      <title>Omelette v1.7.4</title>
      <pubDate>Sat, 11 Jul 2026 19:22:30 +0300</pubDate>
      <sparkle:version>17</sparkle:version>
      <sparkle:shortVersionString>1.7.4</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul>
<li><b>The popover hero ring shows only for a single provider.</b> With several providers the big percentage was anonymous (whose 33%?); the header now stays neutral and the per-provider sections carry the numbers — same rule the menu bar pills follow since 1.7.2.</li>
<li><b>Welcome tour refreshed.</b> The real omelette app icon instead of the old drawn placeholder, the keychain page mentions the "Request keychain access now" fallback button, and the final page notes that updates install themselves.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.4">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.4/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="NubCFeyac4p0A+8ZAiD1q42FBKNrSbXhvc7VWRt4tSCQ9P/7cAHjKZlfDIyDi/VT674vJ6exlLkSV8E7Of85Dw=="
        length="14598505"/>
    </item>
    <item>
      <title>Omelette v1.7.3</title>
      <pubDate>Sat, 11 Jul 2026 19:08:28 +0300</pubDate>
      <sparkle:version>16</sparkle:version>
      <sparkle:shortVersionString>1.7.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Settings → Account → "Request keychain access now".</b> Shows the macOS dialog for the Claude Code-credentials item immediately, skipping the hourly retry limit — the cure for a fresh install stuck on errors until the next automatic prompt.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.3">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.3/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="QUstfDmy0iwk8JVD44eO8qhg+UHsRyBS40JTH+iy2O9kifg9kcJWnYQd/Qy03YBV/8Vf9AazVfDsq7hh9lz5Aw=="
        length="14607369"/>
    </item>
    <item>
      <title>Omelette v1.7.2</title>
      <pubDate>Sat, 11 Jul 2026 18:55:10 +0300</pubDate>
      <sparkle:version>15</sparkle:version>
      <sparkle:shortVersionString>1.7.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul><li><b>Quieter menu bar with several providers</b> — the pills drop their numbers and keep just the colored bars; a lone provider still shows its percent. (The number is that provider's hottest real window — session, weekly or spend limit; promo pools don't count.)</li></ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.2">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.2/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="bQCFoyF9PL+16KDuiadh4LVBQe8H+G8JdAXcQp13KtdZprPSIsfAxYcUamSymY1qvrkc945C1ysjb8DihvaJCw=="
        length="14587893"/>
    </item>
    <item>
      <title>Omelette v1.7.1</title>
      <pubDate>Sat, 11 Jul 2026 18:33:13 +0300</pubDate>
      <sparkle:version>14</sparkle:version>
      <sparkle:shortVersionString>1.7.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li><b>Auto-updates via Sparkle.</b> This build checks the release feed automatically — updates arrive on their own, EdDSA-signed and notarized.</li>
<li><b>Settings → Updates</b> now shows the app version and build number.</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.1">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.1/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="fkys37klc8daLQGJS1X7xQCoMkY2UKvWKOYp93PVU1RumgWsbSP7gZFD2aAUW92LuTrjfcycF1MgWtVTssRmBg=="
        length="14588344"/>
    </item>
    <item>
      <title>Omelette v1.7.0</title>
      <pubDate>Fri, 11 Jul 2026 14:30:00 +0300</pubDate>
      <sparkle:version>13</sparkle:version>
      <sparkle:shortVersionString>1.7.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h3>Changed</h3>
<ul>
<li><b>New app icon</b> — a sunny-side-up egg whose yolk wears a progress ring.</li>
<li><b>README hero banner</b> in the style of Dalí's "The Persistence of Memory".</li>
</ul>
<p><a href="https://github.com/adxd-og/usage-checker/releases/tag/v1.7.0">Full release on GitHub</a></p>
      ]]></description>
      <enclosure
        url="https://github.com/adxd-og/usage-checker/releases/download/v1.7.0/Omelette.dmg"
        type="application/octet-stream"
        sparkle:edSignature="Xy4/z47DRlneuqGC+hrJLNcU5NDOzovzyas6b9MLlxAER3BNu1XszU+yWNexx0puinlhVdoE2ZqnLG7JWlTmCg=="
        length="14601908"/>
    </item>
  </channel>
</rss>
